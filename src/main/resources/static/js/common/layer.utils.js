class LayerUtils {
    static alert(text) {
        alert(text);
    }

    static confirm(text) {
        return confirm(text);
    }

    static prompt(text, defaultText = null) {
        prompt(text, defaultText)
    }
}